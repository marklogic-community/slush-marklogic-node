import ProfileCtrl from './profile.controller';

import template from './profile.html';

const component = {
  bindings: {},
  controller: ProfileCtrl,
  template: template
};

export
default component;
